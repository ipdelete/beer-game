import: "../default.yaml"

weeks: 1
epochs: 1
mode: mechanistic

_list_policy:
  scenarios: replace
  models: replace

scenarios:
  - import: "../scenarios/step_4_8_smoke.yaml"

models:
  - import: "../models/mechanistic.yaml"
