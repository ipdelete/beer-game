import: "../default.yaml"

weeks: 36
epochs: 3
mode: mechanistic

_list_policy:
  scenarios: replace
  models: replace

scenarios:
  - import: "../scenarios/step_4_8_36w.yaml"

models:
  - import: "../models/mechanistic.yaml"
